/**
 * Shared constants used by both apps/web and apps/api.
 * Keep this to values that must genuinely stay in sync across apps.
 */

export const API_VERSION = "v1";
export const API_PREFIX = `/api/${API_VERSION}`;

export const APP_NAME = "Pack & Go - GH";
