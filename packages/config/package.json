{
  "name": "@pack-and-go/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared constants used by both apps/web and apps/api",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
