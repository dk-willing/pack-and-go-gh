# Validators

Zod schemas for validating request bodies/params/query, one file per
resource (e.g. `deliveryRequest.validator.ts`). Validated in a validation
middleware step before the request reaches the controller.

No validators are implemented yet in this foundation stage.
